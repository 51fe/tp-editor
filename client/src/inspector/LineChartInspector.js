import BaseChartInspector from "./BaseChartInspector.js";

export default class LineChartInspector extends BaseChartInspector{
  constructor(editor, name) {
    super(editor, name);
  }

  initForm() {
    super.initForm();
  }
}
